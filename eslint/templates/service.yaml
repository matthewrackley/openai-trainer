apiVersion: v1
kind: Service
metadata:
  name: {{ include "eslint.fullname" . }}
  labels:
    {{- include "eslint.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "eslint.selectorLabels" . | nindent 4 }}
